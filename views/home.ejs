<% if (success && success.length> 0) { %>
    <div class="alert text-center alert-success m-3 mb-0">
        <% success.forEach(msg=> { %>
            <p class="m-0">
                <%= msg %>
            </p>
        <% }) %>
    </div>
<% } %>

<h1 class="mt-3 text-center">Welcome to Gestalt!</h1>
<p class="mb-3">This website is mainly hosted as a planner/email service, combining a daily planner with various functionalities like being able to plan your day
    more efficiently and having access to services local to your region
</p>

